/**
 * 1^1 + 2^2 + 3^3 +....+ 1000^1000
 */

function test(){
    var sum =0;
    for(var i=1;i<=1000;i++){
        sum += Math.pow(i,i);
    }
    console.log(sum);
    //sum
    sum = sum.toString().split("");
    for(var i=)
}

test();

//to be continue... 